# Convenience only. pyproject.toml is the source of truth, including the pinned
# `holidays` version that the festival calendar depends on.
#
#   pip install -e ".[service,models]"
#
duckdb>=1.0
pandas>=2.2
numpy>=1.26
pyarrow>=16.0
