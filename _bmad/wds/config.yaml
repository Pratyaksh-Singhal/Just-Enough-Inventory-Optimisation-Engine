# WDS Module Configuration
# Generated by BMAD installer
# Version: 6.10.0
# Date: 2026-08-09T10:44:38.176Z

project_knowledge: "{project-root}/docs"
project_type: digital_product
design_artifacts: "{project-root}/design-artifacts"
design_system_mode: none
methodology_version: wds-v6
product_languages:
  - en
design_experience: intermediate

# Core Configuration Values
user_name: Pratyaksh
project_name: Inventory_Optimization_Engine
communication_language: English
document_output_language: English
# WDS artefacts live at the project root in phase-numbered folders (wds-0-project-setup,
# wds-1-project-brief, wds-2-trigger-mapping, ...) rather than under _bmad-output. Set
# deliberately on 2026-08-14 so the docs did not have to move.
#
# Note for downstream phases: some WDS guidance refers to `{output_folder}/A-Product-Brief/`
# (see _bmad/wds/data/agent-guides/saga/dream-up-approach.md, Layer 2). Under this
# convention the equivalent path is `{output_folder}/wds-1-project-brief/`. The mapping is
# recorded in wds-0-project-setup/00-project-info.md so no phase has to guess it.
output_folder: "{project-root}"
wds_folder_convention: "wds-{phase}-{name}"
